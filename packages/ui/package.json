{
  "name": "ui",
  "version": "0.0.1",
  "sideEffects": [
    "*.css"
  ],
  "private": true,
  "types": "./types/index",
  "main": "src/index.ts",
  "module:jsx": "src",
  "files": [
    "types",
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist types && tamagui-build",
    "watch": "tamagui-build --watch ",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@tamagui/themes": "latest",
    "tamagui": "latest",
    "tamagui-phosphor-icons": "latest"
  },
  "devDependencies": {
    "@tamagui/build": "latest"
  }
}
